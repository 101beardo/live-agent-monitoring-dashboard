node_modules/
.next/
out/
*.log
.DS_Store
.env*
*.tsbuildinfo
.claude/

# Auto-regenerated by `next dev`/`next build` (Next 16 agent-guidance files) —
# ignored so a fresh `npm run dev` doesn't dirty the tree for a reviewer.
AGENTS.md
CLAUDE.md
